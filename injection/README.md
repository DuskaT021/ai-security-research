# Injection (XSS & SQLi)

Documents how AI-assisted code often skips input handling: raw props in UI layers and string-built queries in data layers. This section holds small demos you can run, compare, and use as regression patterns in reviews.

- [`xss-demo/`](./xss-demo/) — vulnerable React (or similar) component vs a fixed version
- [`sqli-demo/`](./sqli-demo/) — raw query construction vs parameterized queries

**Results / status:** WIP — findings and reproduction steps added as demos land here.
