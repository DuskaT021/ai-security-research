# Prompt injection

Collects payloads and outcomes when untrusted content (docs, filenames, comments) steers coding agents or inline LLMs toward unsafe behavior.

- [`payloads/`](./payloads/) — tested strings, contexts, and what happened (leak, tool abuse, etc.)

**Results / status:** WIP — payloads and notes appended as tests run.
